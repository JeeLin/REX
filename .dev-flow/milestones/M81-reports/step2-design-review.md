# 步骤2 设计核对：M81

## 审查维度

| # | 维度 | 结论 | 说明 |
|---|------|------|------|
| 1 | 单用户/自托管边界 | ✅ | SQL 查询保存使用本地 `settings` 表（单用户），文档明确「不做云端同步/跨设备共享」「不引入多用户/RBAC/团队协作」（M81 产品边界 + 设计核对点）。符合 PRODUCT.md 第 48-49 行单用户/自托管定位。 |
| 2 | 深色优先设计系统一致性 | ✅ | 前端改动（SQL 命名列表 UI、SSH 初始化脚本输入框）均要求使用设计 token（`--bg-elevated`/`--border`/`--text-*`），明确「不引入硬编码 hex」；复用 `components/ui/*`。与 M78/M80 既有设计系统一致。 |
| 3 | 不引入 RBAC/多用户 | ✅ | 无权限模型、无多用户、无团队概念。SQL 保存为全局命名列表但仅本地单用户可见，不引入访问控制。 |
| 4 | 文件传输不经过浏览器 | ✅ | SSH 初始化脚本（`init_script`）仅在终端会话建立后在远端逐行执行，属终端交互增强，不经浏览器；不改变 rex-transfer 文件传输通道（数据仍 Hub/Agent/远端之间传输）。未触碰文件传输架构。 |
| 5 | Hub-Agent 版本一致 | ✅ | 更新检查修复（#5）仅改变「如何挑选 latest 版本」的逻辑，仍要求 Hub 与 Agent 版本严格一致；未引入跨版本兼容。SQL 保存 / SSH init_script 均为 Hub 侧功能，不影响 Agent 协议。 |

## 汇总

- **通过维度**：5/5
- **结论**：✅ 通过

## 发现的问题

无

## 补充说明

- 产品对标（Navicat 查询文件、Xshell 初始化脚本）属于交互逻辑复刻，符合 PRODUCT.md 第 13-15 行的设计基调（对标成熟工具的操作方式，非外观移植）。
- 🔴 更新检查降级修复补充了单元测试（语义化版本比较：0.68.0 vs 0.65.4 不更新、1.2.0 vs 1.10.0 选 1.10.0），覆盖边界清晰。
- 三个 M80 缺陷（降级/分页/分栏聚焦）已纳入 Bugs 表并关联对应修复子任务（#5/#6/#7），归属正确。
