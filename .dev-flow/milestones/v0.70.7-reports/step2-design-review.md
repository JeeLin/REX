# v0.70.7 步骤2：设计核对（里程碑文档 vs 产品文档）

> 触发：步骤1 完成，Flow Status 步骤2 未勾选。
> 审查对象：`docs/milestones/v0.70.7-sql-resource-merge.md`（里程碑文档）vs `docs/PRODUCT.md`（产品文档）。
> 审查维度（取自 `AGENTS.md` / `CLAUDE.md`）：单用户自托管、Hub/Agent 版本一致、文件传输数据不经浏览器、不引入多用户/RBAC/企业协作、不跳阶段、不污染产品文档、产品语义与用户可见行为不变。

## 核对项

| # | 审查维度 | 结论 | 依据 |
|---|----------|------|------|
| 1 | 单用户 / 自托管定位 | ✅ | 文档未引入多用户/RBAC/协作概念；产品边界明确写「不引入多用户/RBAC/企业协作」 |
| 2 | Hub/Agent 版本一致 | ✅ | 沿用 v0.70.6 隧道契约；本里程碑仅资源模型层改动，Hub/Agent 同步 bump 至 v0.70.7，无跨版本兼容问题 |
| 3 | 文件传输数据不经浏览器 | ✅ | 本里程碑**不涉及文件传输变更**；仅改连接树/创建向导/SQL 控制台路由（前端）与资源模型/探测（后端），不触碰 Files/S3 数据传输路径 |
| 4 | 不跳过阶段 | ✅ | 严格 8 步串行；步骤1 已完成并打 `milestone-v0.70.7-start` |
| 5 | 不污染产品文档 | ✅ | 当前仅提交里程碑文档 + DEVELOPMENT.md 条目；PRODUCT.md 现状改动（60 行重写）为既有未跟踪项，未纳入本里程碑提交 |
| 6 | 产品语义 / 用户可见行为不变 | ✅ | 资源从「3 类并列」收敛为「单一 SQL + dialect 自动识别」；用户创建体验简化（连时自动识别），SQL 控制台 Navicat 对标行为不变 |
| 7 | 范围边界明确（用户强约束） | ✅ | **「当前仅支持 MySQL / PostgreSQL / SQLite 三种；新方言不在范围」已硬约束写入「产品边界」+「子任务2」+「设计核对点」三处**，非口头承诺；探测表预留扩展位但本次不实现、不引入新依赖 |
| 8 | 向后兼容 / 无破坏性 | ✅ | 旧 mysql/postgresql/sqlite 资源 in-place 升级为 `protocol='sql'` + `db_type=<旧protocol>`（子任务5，幂等迁移）；旧客户端不传 `db_type` 时按 `None` 处理 |

## 发现

无 🔴 / 🟡 / 🟢 问题。设计与产品文档一致，范围边界清晰，用户强约束（仅 3 种 SQL）已落到文档三处。

## 结论

✅ 设计核对通过。勾选步骤2，进入步骤3 开发。
