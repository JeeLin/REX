# 功能待办（Backlog）

记录已规划/酝酿中、尚未排入具体里程碑的**功能向**待办。缺陷类（bug）请写入 `docs/BUGS.md`，本文件只收 feature。

里程碑规划时（milestone-planner）可从此处吸纳高优先级功能。

| 提出版本 | 优先级 | 标题 | 描述 |
|----------|--------|------|------|
| 0.69.0 | 🟢 | 每种资源实现自己的工具箱（Toolbox） | 期望按资源协议类型在各自 `features/<protocol>/` 域下提供专属工具面板，如 SSH 支持「一键更换 apt 源」（复用 M81 的 `init_script`「连接后执行脚本」能力，把脚本从手填改为内置工具按钮触发）。可先以 SSH 换 apt 源为首例，后续扩展至 Redis 内存清理、MySQL 慢查询分析等。 |
