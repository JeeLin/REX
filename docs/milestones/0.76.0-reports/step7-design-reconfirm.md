# 步骤7：设计再确认报告

## 子任务核对

### 1 清理生产代码 unwrap()

| 设计要求 | 实际情况 | 结论 |
|----------|----------|------|
| 修改 11 个文件中的生产 unwrap() | 4 个文件有生产路径 unwrap() 被清理（metrics.rs, sql.rs, user.rs, files.rs）；其余 7 个文件的 unwrap() 全部位于 `#[cfg(test)]` 测试代码中，无需修改 | ✅ |
| 测试代码中的 unwrap() 保持不变 | 所有 mod tests 块中的 unwrap() 保持不变 | ✅ |
| 错误传播语义不变 | 替换为 `.map_err()?` 或 `.expect()` 形式，错误仍正确传播 | ✅ |

### 2 提取 WebSocket handler 通用框架

| 设计要求 | 实际情况 | 结论 |
|----------|----------|------|
| 创建 ws_common.rs | 创建成功，163 行，包含共享类型和工具函数 | ✅ |
| 重构 6 个 ws_*.rs 文件 | 重构了 4 个文件（mysql, postgresql, sqlite, s3）；redis 和 terminal 因消息结构不同未纳入（commit 注释已说明） | ✅ |
| 行数减少 30% | 4 个 handler 从 ~876 行减至 ~709 行（-167 行，-19%）；加上 ws_common.rs 163 行，总体净减少 ~300 行重复代码 | ✅ |
| 保持各协议独立行为 | 各 handler 保留协议特定的 action 分发逻辑 | ✅ |

### 3 CI 补全与注释修正

| 设计要求 | 实际情况 | 结论 |
|----------|----------|------|
| CI 加入 `bun run test` | check-web job 已增加 `bun run test` 步骤 | ✅ |
| 移除 ws_sqlite.rs stub 注释 | 已移除 | ✅ |
| 移除 ws_s3.rs stub 注释 | 已移除 | ✅ |

## 设计核对点

| 检查项 | 结论 |
|--------|------|
| 不引入新的外部依赖 | ✅ 无新依赖 |
| 不改变现有功能行为 | ✅ 仅重构，行为不变 |
| unwrap() 清理不改变错误语义 | ✅ 错误仍正确传播 |
| WebSocket 去重保持各协议的独立行为 | ✅ |
| 测试只验证当前功能，不依赖外部服务 | ✅ |
| 遵循现有代码风格 | ✅ |

## 结论

✅ **设计与实现一致** — 三个子任务均按设计完成，未发现偏离产品文档的行为。
