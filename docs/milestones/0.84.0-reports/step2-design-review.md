# 步骤2：设计核对报告 — 0.84.0 审计日志增强

## 审查维度

### 1. 产品文档一致性 ✅

| 检查项 | 结论 |
|--------|------|
| §3.10 操作类型颜色编码 | ✅ 前端 audit-op 样式已按协议/操作类型着色（connect=绿色、query=蓝色、upload=紫色等），子任务3不改变此逻辑 |
| §3.10 筛选条件 | ✅ 子任务2新增的操作类型（connect/disconnect/query/upload/download）已在前端 operationTypes 列表中 |
| §3.10 统计卡片 | ✅ 统计逻辑基于 result 字段聚合，与操作类型无关，不受影响 |
| §3.10 详情展开 | ✅ 子任务3从原始 key-value 升级为格式化网格，符合 §3.10"结构化详情网格（协议、目标、用户、耗时、传输量等）" |
| §3.10 CSV 导出 | ✅ 导出逻辑不变，仅操作类型值更丰富 |
| §3.10 分页 | ✅ 分页逻辑不变 |

### 2. 数据模型兼容性 ✅

| 检查项 | 结论 |
|--------|------|
| audit_log 表结构 | ✅ 不新增列，仅 detail_json 内容更丰富 |
| 现有审计记录 | ✅ 不迁移旧数据，新字段为向后兼容（None 时保持原行为） |
| API 响应格式 | ✅ AuditLogEntry 结构不变，detail 字段仍为 Option<String> |

### 3. 接口面收敛 ✅

| 检查项 | 结论 |
|--------|------|
| write_audit_log 签名变更 | ✅ 新增参数均为 Option（env_name, resource_name），现有调用无需立即迁移 |
| 前端无新 API | ✅ 不新增 API 端点，仅数据内容变化 |
| WebSocket handler 签名 | ✅ 不改变 WebSocket handler 的公共签名，审计在 handler 内部调用 |

### 4. 向后兼容 ✅

| 检查项 | 结论 |
|--------|------|
| 旧审计记录展示 | ✅ 旧记录 detail_json 可能为空或格式不同，前端 detail 空时显示"暂无详细信息" |
| 新旧操作类型混存 | ✅ 前端 operationTypes 列表已包含新旧所有类型，筛选兼容 |

### 5. 性能影响 ✅

| 检查项 | 结论 |
|--------|------|
| 同步写入 | ✅ 保持现有 `let _ = conn.execute(...)` 模式，审计写入失败不影响主流程 |
| WebSocket 断开时写入 | ✅ 在 socket.close() 后同步写入，不阻塞连接清理 |
| 查询开销 | ✅ 连接建立时需查询 resource 表获取名称，单次查询开销可忽略 |

### 6. 安全性 ✅

| 检查项 | 结论 |
|--------|------|
| 敏感信息 | ✅ detail_json 不记录密码/密钥内容，仅记录元信息（host、port、protocol） |
| SQL 注入 | ✅ 使用 rusqlite 参数化查询，无注入风险 |

## 发现

### 🟢 可选改进

1. **子任务1 write_audit_log 签名变更影响范围**：当前调用点分布在 auth.rs、env.rs、resource.rs 3个文件共10处。建议在子任务1中一次性迁移所有调用点，避免新旧签名混用。

### 结论

✅ 里程碑文档设计合理，与产品文档一致，数据模型兼容，无大问题。可进入步骤3开发。
