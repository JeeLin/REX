# M18 步骤7：设计再确认报告

## 实现 vs 里程碑文档

| 子任务 | 里程碑描述 | 实际实现 | 一致性 |
|--------|-----------|----------|--------|
| 18.3 终端右键菜单 | 复制/粘贴/全选/清屏/重连/打开SFTP/新建连接/复制地址/断开连接 | ✅ 全部 9 个菜单项实现，使用 useContextMenu composable | ✅ |
| 18.2 SQL 编辑器右键菜单 | 执行选中/全部/剪切/复制/粘贴/格式化/大小写转换/注释/保存/插入模板/历史 | ✅ 全部菜单项实现，含子菜单（大小写转换、插入模板） | ✅ |
| 18.2 SQL 结果右键菜单 | 复制行/单元格/整列/JSON/排序/导出/生成UPDATE/DELETE | ✅ 全部菜单项实现 | ✅ |
| 18.1 SSH 终端内置 SFTP 面板 | toggle按钮/split view/可拖拽分隔条/文件浏览/拖拽到终端 | ✅ 全部功能实现 | ✅ |

## 额外变更

| 变更 | 说明 | 一致性 |
|------|------|--------|
| `useSqlTabActions` composable | 提取 SQL 标签页管理逻辑，消除 WorkspaceSql/SqlConsole 重复代码 | ✅ 精简不改变功能 |
| `FileList.vue` draggable | 添加 `draggable` 和 `@dragstart` 支持拖拽到终端 | ✅ 与 SFTP 面板功能直接相关 |

## 审查维度

### 1. 产品定位一致性
- ✅ 单用户、自托管、深色优先
- ✅ 未引入多用户、RBAC、企业协作

### 2. 架构一致性
- ✅ 前端功能域改动，不涉及后端架构变化
- ✅ 文件传输不经过浏览器原则未改变（SFTP 面板仅浏览文件列表，不传输文件数据）

### 3. 前端交互
- ✅ 右键菜单使用现有 useContextMenu composable（singleton 模式）
- ✅ 复制操作使用 navigator.clipboard API
- ✅ SFTP 面板复用 FileList.vue 组件
- ✅ 拖拽分隔条调整面板宽度

### 4. i18n
- ✅ zh.ts 和 en.ts 均已添加所有新增键
- ✅ 命名规范一致（`ws.terminal.ctx.*`、`sql.ctx.*`、`sql.result.ctx.*`、`files.sftp.*`）

### 5. 与里程碑文档一致性
- ✅ 三个子任务的功能目标、修改文件、交互设计均与文档一致
- ✅ 排除项（跨连接传输、AI 助手、全局查询 modal）未被实现

## 门禁结论

✅ 设计再确认通过
