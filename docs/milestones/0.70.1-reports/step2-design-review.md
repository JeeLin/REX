# 步骤2：设计核对 — 0.70.1 浏览器实时双向音频

> 审查对象：里程碑文档 `0.70.1-browser-real-time-audio.md` vs 产品文档 `docs/PRODUCT.md`。
> AGENTS.md 无 `## 审查维度`，按 CLAUDE.md 开发流程的产品核对维度 + PRODUCT.md 原则执行。

## 审查维度

| # | 维度 | 结论 | 说明 |
|---|------|------|------|
| 1 | 单用户、自托管定位不被破坏 | ✅ | 音频端点仍是单一用户本地浏览器，无多租户/协作概念；沿用 rex-sip + `/ws/sip` + agent 隧道 |
| 2 | 不引入 RBAC / 多用户 / 企业协作 | ✅ | 文档「产品边界 · 本阶段不做什么」与「设计核对点」均显式声明不引入，符合 PRODUCT.md L48 单用户原则 |
| 3 | 架构一致性（单二进制 + supervisor + worker） | ✅ | 复用 M82a 的 rex-sip（UA₁/UA₂）、`/ws/sip`、`agent_ws.rs` 隧道；无新进程模型；opus 仅作线上编解码依赖 |
| 4 | 文件传输不经过浏览器（PRODUCT.md L430） | ✅ | L430 属**文件传输架构**上下文（L428-434）。音频实时流是「媒体不经过浏览器」原则的**显式例外**（DEVELOPMENT.md M82b 已确认：用户明确要求浏览器实时听/说）；且音频帧仍经 Hub/Agent 服务端中转，浏览器仅作媒体端点、不接触远端 SIP 服务器——与「文件传输不经过浏览器」初衷不冲突。里程碑文档「设计核对点」已显式标注此例外 |
| 5 | 不跳阶段 / 按规划递进 | ✅ | M82b 紧接 M82a（0.70.0），与 DEVELOPMENT.md 里程碑划分一致；音频为信令层（M82a）的自然延续 |
| 6 | 实现细节不污染产品文档 | ✅ | PRODUCT.md 未改动；音频例外在 DEVELOPMENT.md M82b 记录，里程碑文档引用，分层一致 |
| 7 | 向后兼容 / 无破坏性变更 | ✅ | 信令层（register/dial/.../事件流）行为不变，本里程碑仅增媒体通道；版本号 0.70.1（minor 段递增，向后兼容） |

## 汇总

- **通过维度**：7/7
- **结论**：✅ 通过

## 发现的问题

无（无 ❌ 维度）。

> 备注：PRODUCT.md L430「数据在 Hub/Agent/远端之间传输，不经过浏览器」易被误读为禁止任何浏览器媒体。经核对其上下文为文件传输架构，且 DEVELOPMENT.md M82b 已明确本里程碑为显式例外，故不视为设计偏离。如后续希望产品文档更清晰，可在 PRODUCT.md 文件传输段补充一行「实时音频流除外」说明（属可选改进，不阻断本步）。
