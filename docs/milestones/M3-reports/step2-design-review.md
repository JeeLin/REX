# M3 步骤2：设计核对报告

## 审查维度

### 1. 产品边界
- ✅ SSH 终端核心功能覆盖（xterm.js + WS 桥接 + 查找 + 右键 + 主题 + 移动端）
- ✅ SFTP 抽屉明确推迟到 M6，不在此实现
- ✅ 未引入 SSH 隧道/端口转发等超出范围的功能

### 2. 架构一致性
- ✅ WebSocket 桥接模式符合 `connection-channels.md` 设计（浏览器 → Hub WS → 协议 crate → 目标）
- ✅ 消息协议遵循 `api-design.md` 的 REXMessage 结构
- ✅ 后端使用 russh（Rust 标准 SSH 库），技术选型合理

### 3. 子任务拆分
- ✅ 7 个子任务，粒度适当（每个 1-2 commit）
- ✅ 后端（子任务1）与前端（子任务2-6）职责清晰
- ✅ 前端子任务可独立开发和测试

### 4. 前后端对齐
- ✅ WebSocket 消息类型前后端协议一致
- ✅ resize/断开/重连流程完整

### 5. 依赖关系
- ✅ 依赖 M2（工作空间外壳），正确
- ✅ xterm.js 已在 package.json 中，无需额外安装
- ⚠️ russh 需添加到 Cargo.toml（子任务1 中已注明修改 Cargo.toml）

### 6. 单用户/自托管
- ✅ 无多用户、RBAC 概念引入

### 7. 版本号
- ✅ 0.3.0 → 0.4.0（minor，新功能）

## 结论：✅ 通过

设计合理，无大问题。子任务1（后端）范围稍大但 SSH crate + WS 桥接紧密耦合，拆开反而增加集成成本，保持现状。
