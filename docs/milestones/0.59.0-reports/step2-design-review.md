# 步骤2：设计核对报告

## 里程碑：0.59.0 SQL 控制台移动端浮动工具栏

## 审查维度

### 1. 产品定位

| 检查项 | 结果 |
|--------|------|
| 单用户设计：无权限检查 | ✅ 无权限检查逻辑 |
| 自托管：所有功能本地运行 | ✅ 纯前端组件，无外部依赖 |
| 不引入多用户/RBAC | ✅ 无 |

### 2. 架构一致性

| 检查项 | 结果 |
|--------|------|
| 无后端变更 | ✅ 纯前端组件 |
| 单二进制+supervisor 模型 | ✅ 不涉及 |
| 文件传输不经过浏览器 | ✅ 不涉及 |

### 3. 与已有模式一致性

| 检查项 | 结果 |
|--------|------|
| 复用 TerminalMobileToolbar 模式 | ✅ CSS、Props、Emits 模式一致 |
| i18n key 命名规范 | ✅ 已修正为 `sql.mobile.*`（SQL key 在顶层，不在 `ws.*` 下） |
| 文件结构 | ✅ `features/sql/` 目录下，与项目约定一致 |
| 移动端检测方式 | ✅ `window.innerWidth < 768`，与 Files.vue 一致 |

### 4. 产品边界

| 检查项 | 结果 |
|--------|------|
| 不修改后端 SQL API | ✅ 明确声明 |
| 不改变桌面端行为 | ✅ `isMobile` 控制可见性 |
| 不修改编辑器核心逻辑 | ✅ 仅 emit 事件 |

### 5. 子任务拆分

| 检查项 | 结果 |
|--------|------|
| 3 个子任务，粒度合理 | ✅ 组件→集成→测试 |
| 每个子任务 1-2 个 commit | ✅ |
| 依赖关系清晰 | ✅ 依赖 0.58.0 |

## 发现问题

### 🟡 i18n key 命名（已修正）

里程碑文档初稿使用了 `ws.sql.mobile.*`，但 SQL 的 i18n key 在顶层 `sql.*` 下（不在 `ws.*` 下）。已修正为 `sql.mobile.*`。

## 结论

✅ 通过。里程碑文档与产品文档一致，设计合理，无大问题。
