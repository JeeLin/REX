# 0.2.0 步骤2：设计核对报告

## 审查范围

里程碑文档 `0.2.0-terminal-sftp-and-polish.md` vs `docs/PRODUCT.md`

## 逐项核对

### 1. 终端内置 SFTP 面板

| 检查项 | 产品文档 | 里程碑文档 | 结果 |
|--------|----------|-----------|------|
| SFTP 面板为 split view | §3.6：「在右侧/下方打开内嵌 SFTP 面板（split view）」 | split view，终端占 60%，SFTP 占 40% | ✅ |
| 共享 SSH 连接 | §3.6：「共享同一个 SSH 连接」 | 复用同一 resource_id 的文件 API | ✅ |
| 文件列表交互 | §3.6：「独立浏览远程文件系统」 | 双击进入/返回，右键菜单 | ✅ |
| 拖拽文件到终端 | §3.6：「从 SFTP 面板拖拽文件到终端区域，自动将文件路径粘贴到终端」 | 拖拽文件到终端区域 | ✅ |
| API 路径 | 后端已有 `/api/resources/:resource_id/files` | ~~原设计用 `/sftp/*`~~ 已修正为 `/files/*` | ✅（已修正） |
| SFTP 面板可拖拽调整大小 | §3.6：「SFTP 面板可拖拽调整大小」 | 可选功能 | 🟡 可选 |
| 不引入多用户 | §1：单用户 | 无多用户概念 | ✅ |
| 文件传输不经过浏览器 | §3.8 传输方式 | REST API 直连后端 | ✅ |

### 2. 终端全屏按钮

| 检查项 | 产品文档 | 里程碑文档 | 结果 |
|--------|----------|-----------|------|
| 全屏按钮位置 | §3.6 工具栏右侧：「⛶ 全屏」 | 工具栏添加 ⛶ 按钮 | ✅ |
| 实现方式 | §3.6：F11 全屏切换 | `requestFullscreen()` API | ✅ |
| 死代码清理 | — | 清理 `handlePasteEvent` | ✅ 无违规 |

### 3. Settings 个人信息区块

| 检查项 | 产品文档 | 里程碑文档 | 结果 |
|--------|----------|-----------|------|
| 个人信息区块 | §3.11：「用户名 · 编辑按钮」「密码 · 上次修改时间 · 修改按钮」 | ProfileSection 组件 | ✅ |
| 修改用户名 | §3.11：编辑按钮 | PUT /api/user/profile | ✅ |
| 修改密码 | §3.11：修改按钮 | PUT /api/user/password，需验证当前密码 | ✅ |
| 单用户概念 | §1：单用户 | 不引入多用户 | ✅ |

### 4. Dashboard 审计统计 + SQL 消息标签 + 标签关闭改进

| 检查项 | 产品文档 | 里程碑文档 | 结果 |
|--------|----------|-----------|------|
| 今日操作数 | §3.2：todayOps 统计卡片 | GET /api/audit/stats?period=today | ✅ |
| SQL 消息标签 | §3.7 结果标签：「结果、消息、执行计划」 | 消息 tab，展示执行结果/错误 | ✅ |
| 关闭全部标签 | §3.7 查询标签右键菜单：「关闭所有已打开的查询标签」 | 关闭全部 | ✅ |
| 关闭已保存的 | §3.7：「关闭已保存的查询标签（保留未保存的）」 | 关闭已保存的 | ✅ |
| 不引入执行计划 | §3.7 定义但不在本里程碑 | 明确标注不实现 | ✅ |
| 不引入全局查询 | §3.7 定义但不在本里程碑 | 明确标注不实现 | ✅ |
| 不引入 AI 助手 | §3.7 定义但不在本里程碑 | 明确标注不实现 | ✅ |

## 架构一致性

| 检查项 | 结果 |
|--------|------|
| 单用户、自托管 | ✅ 未引入多用户/RBAC |
| 单二进制 + supervisor + worker | ✅ 后端仅添加 handler |
| 文件传输不经过浏览器 | ✅ REST API 直连后端 |
| 深色优先 | ✅ 新组件遵循现有主题 |
| 版本兼容 | ✅ Hub + Agent 版本一致要求不变 |

## 小问题修正

| 问题 | 修正 |
|------|------|
| SFTP 面板 API 路径使用不存在的 `/sftp/*` | 已修正为后端实际存在的 `/files/*` REST API |

## 结论

✅ 通过。里程碑文档设计与 PRODUCT.md 一致，产品边界未被污染，仅修正了一个 API 路径小问题。
