# 0.2.0 步骤5：代码审查报告

## 审查范围

`git diff main~5..main` — 本次里程碑的所有变更（17 个文件，~700 行新增）

## 审查维度

### 正确性

| # | 严重度 | 文件 | 行 | 发现 | 失败场景 |
|---|--------|------|-----|------|----------|
| 1 | 🟡 | user.rs | 62 | `input.username.len()` 按字节计数，中文字符 3 字节 | 22 个中文字符 = 66 字节，超过 64 限制被拒绝，但错误提示说"64 个字符" |
| 2 | 🟢 | helpers.rs | 169 | `get_or_create_password_hash` 并发调用可能生成不同默认哈希 | 两个请求同时无哈希时，各自生成不同 salt，INSERT OR REPLACE 后只有一个生效。单用户场景无实际影响 |

### 安全性

| # | 严重度 | 文件 | 行 | 发现 | 说明 |
|---|--------|------|-----|------|------|
| 3 | 🟢 | user.rs | 34 | `change_password` 无速率限制 | 暴力破解当前密码。单用户自托管场景风险低，但建议后续添加 |

### 架构一致性

| # | 严重度 | 文件 | 行 | 发现 | 说明 |
|---|--------|------|-----|------|------|
| 4 | 🟡 | ProfileSection.vue | 全文件 | 硬编码中文字符串，未使用 i18n | 项目其他组件均使用 `useI18n()` + i18n 键，此组件 15+ 处硬编码中文 |

### 测试覆盖

| # | 严重度 | 文件 | 行 | 发现 | 说明 |
|---|--------|------|-----|------|------|
| 5 | 🟡 | audit.rs | 155 | `get_stats` 仅测试 `period=None`，未测试 `period=today` | today 分支的日期前缀计算和 WHERE 过滤未被覆盖 |
| 6 | 🟢 | user.rs | 55 | `update_profile` 未测试 `len > 64` 的拒绝路径 | 边界条件未覆盖 |

### 清理

| # | 严重度 | 文件 | 行 | 发现 | 说明 |
|---|--------|------|-----|------|------|
| 7 | 🟢 | SqlResults.vue | 77 | `computed` 导入但未使用 | 编译器警告，可安全移除 |

## 结论

✅ 通过。无 🔴 必须修复项。

**建议修复（🟡）：**
- #1：`username.len()` 改为 `username.chars().count()` 或使用 `chars().count() > 64`
- #4：ProfileSection.vue 的硬编码中文改为 i18n 键
- #5：补充 `get_stats` 的 `period=today` 测试

**可选改进（🟢）：**
- #2：并发默认哈希生成 — 单用户场景无需处理
- #3：密码暴力破解保护 — 后续里程碑考虑
- #6：补充 `len > 64` 测试
- #7：移除未使用的 `computed` 导入
