# M19 步骤2：设计核对报告

**日期：** 2026-06-21

## 审查范围

M19 里程碑文档 vs PRODUCT.md §3.7

## 审查结果

### 1. 库表结构树右键菜单

**§3.7 定义 vs M19 设计：**

| 节点 | §3.7 定义 | M19 实现 | 状态 |
|------|----------|---------|------|
| 表节点 | 查看表结构、查看行数统计、复制表名、SELECT*查询、导出表数据、刷新 | 查看表结构、查看行数统计、复制表名、SELECT*查询 | ⚠️ 缺少 2 项 |
| 列节点 | 复制列名、复制列类型、查看列约束 | 复制列名、复制列类型 | ⚠️ 缺少 1 项 |
| 数据库节点 | 刷新、新建表、复制数据库名 | 刷新、复制数据库名 | ⚠️ 缺少 1 项 |
| 视图节点 | 查看定义、复制视图名、刷新 | （排除） | ✅ 合理 |
| 空白区域 | 全部展开、全部折叠、刷新结构 | 全部展开、全部折叠、刷新结构 | ✅ 一致 |

**缺少项分析（均为后端 API 未实现）：**

| 缺少项 | 原因 | 严重程度 |
|--------|------|----------|
| 表节点「导出表数据」 | 需要后端流式导出 API | 小 — 已标注排除 |
| 表节点「刷新」 | 当前无实时刷新需求 | 小 — 已标注排除 |
| 列节点「查看列约束」 | 需要后端约束查询 API | 小 — 已标注排除 |
| 数据库节点「新建表」 | 需要 DDL 执行 API | 小 — 已标注排除 |

### 2. 查询标签右键菜单

**§3.7 定义 vs M19 设计：**

| §3.7 定义 | M19 实现 | 状态 |
|----------|---------|------|
| 关闭、关闭其他、关闭已保存的 | 关闭、关闭其他 | ⚠️ 缺少 1 项 |
| 保存、另存为…、重命名 | 保存、重命名 | ⚠️ 缺少 1 项 |
| 复制 SQL、执行 SQL | 复制 SQL、执行 SQL | ✅ 一致 |

**缺少项分析：**

| 缺少项 | 原因 | 严重程度 |
|--------|------|----------|
| 关闭已保存的 | 需要保存状态追踪 | 小 — 已标注排除 |
| 另存为… | 需要查询文件保存流程 | 小 — 已标注排除 |

### 3. i18n 覆盖

- ✅ `sql.tree.ctx.*` 覆盖所有新增文字（11 个键）
- ✅ `sql.tab.ctx.*` 覆盖所有新增文字（6 个键）
- ✅ 命名规范与现有 `sql.ctx.*`、`sql.result.ctx.*` 一致

### 4. 技术实现

- ✅ 使用 `useContextMenu()` composable（singleton 模式）
- ✅ 复制操作使用 `navigator.clipboard` API
- ✅ 视图节点菜单标注为后续实现（后端未支持）

### 5. 修正记录

审查过程中发现的小问题已直接修正里程碑文档：

1. 补充「不做什么」列表，明确标注所有与 §3.7 的差异及原因
2. 空白区域菜单「刷新」→「刷新结构」（与 §3.7 措辞一致）
3. 新增 `sql.tree.ctx.refresh` 键（数据库节点刷新）
4. 澄清「查看表结构」行为：展开/折叠该表节点

## 门禁结论

✅ **设计核对通过** — 所有差异项均为后端 API 未实现导致的合理排除，已在文档中明确标注。
