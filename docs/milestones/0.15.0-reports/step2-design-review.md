# 设计核对报告 — 0.15.0 跨连接文件传输

## 检查项

1. **产品定位**：✅ 完全符合单用户、自托管定位，无 RBAC/多用户概念
2. **架构一致性**：✅ 遵循 Hub 单二进制架构，传输在 `rex-transfer` crate 中执行
3. **文件传输设计**：✅ 严格遵循"传输数据不经过浏览器"原则，Hub 后端直接流转
4. **功能边界**：✅ 不跳阶段，不引入调度器/并发控制，范围合理
5. **子任务拆分**：✅ 三个子任务粒度适当，依赖关系正确（3 依赖 1+2，1 和 2 无依赖）

## 问题

- [🟡 已修复] 内存一次性读取可能对大文件造成压力 → 已明确本里程碑先用全量读写，流式传输作为后续优化
- [🟡 已修复] 冲突处理未明确 → 已在"不做什么"中声明目标文件已存在时直接覆盖
- [🟡 已修复] `sftp` + 连接参数回退路径未实现 → 已移除该路径，仅支持 `resource_id` 模式

## 结论

✅ 通过
