# Step 4: 代码精简

## 检查范围

M14 子任务1-5的全部变更（23个文件，+979/-218行）。

## 检查维度

### 1. 重复代码

- **翻译文件**：zh.json 和 en.json 结构完全对称，无重复
- **页面 $t() 调用**：各页面独立使用 `useI18n()` + `t()`，无重复模式可提取
- **加密/解密**：resource_api.rs 中 create/update 加密逻辑相同，但仅2处，提取函数无必要

### 2. 过度设计

- **crypto.rs**：4个测试覆盖核心路径（roundtrip、不同nonce、错误密钥、密钥持久化），无过度测试
- **App.vue 主题恢复**：使用动态 import 避免循环依赖，合理
- **底部导航**：纯 CSS 实现，无 JS 复杂度

### 3. 未使用代码

- 无发现

### 4. 依赖规则

- `aes-gcm` 和 `tempfile` 正确使用 workspace = true
- 无重复声明

## 结论

精简不改变功能行为。代码组织合理，无需要精简项。
