# v0.70.8: Hub / Agent 服务化管理（CLI 子命令 + 开机自启）

## Context

前序阶段（v0.70.0–v0.70.7）完成了 SIP 电话、统一隧道架构、SQL 资源模型合并，产品功能已稳定。当前 Hub / Agent 两个二进制仍以「env 变量 + 直接运行可执行文件」的方式启动：配置靠 `REX_PORT` / `REX_DATA_DIR` / `REX_HUB_URL` / `REX_AGENT_TOKEN` 等环境变量，启动方式是手工 `./rex-hub`，没有子命令入口，也没有任何开机自启机制。

本阶段补齐自托管运维最基础的一环：**统一的 CLI 子命令入口** + **一键注册为操作系统服务实现开机自启**。这让 `rex-hub` / `rex-agent` 的运维体验和 Xshell / Navicat 这类成熟工具的「安装为服务」对齐（对标 PRODUCT §10 的「部署/运维」基线），也是后续容器化之外最自然的二进制部署形态。

版本类型：minor（新增功能，向后兼容。现有 env 变量配置全部保留，行为不变；只是新增 `run`/`version`/`service` 子命令入口，默认子命令即原有启动逻辑）。

## 产品边界

本阶段做什么：
- 为 Hub / Agent 引入基于 `clap` derive 的 CLI 子命令：`run`（默认，启动 supervisor+worker）、`version`、`service install/uninstall/start/stop/restart/status`。
- 实现「一键注册为系统服务」并开机自启：
  - Linux → systemd（user 与 system 两种 scope）。
  - macOS → launchd（user `LaunchAgents` 与 system `LaunchDaemons` 两种 scope）。
- `service install` 把当前相关 env 变量（Hub：`REX_PORT` / `REX_DATA_DIR` / `REX_STATIC_DIR`；Agent：`REX_HUB_URL` / `REX_AGENT_TOKEN`）写入生成的单元文件，使服务以与当前手工启动一致的方式运行。
- 更新部署文档，说明二进制部署下的 `service install` 与开机自启。

本阶段不做什么：
- 不改动 supervisor + worker 进程模型、退出码语义、更新/回滚机制（沿用 `rex-common::supervisor` 与 `rex-agent::supervisor`）。
- 不实现 Windows 服务注册自动化：Windows 仅给出带外指引（nssm / 任务计划程序），CLI 在 Windows 上对这些子命令返回明确「unsupported」错误与提示。原因：Windows 已有 supervisor.exe 自我复制模型，自动化注册需 `sc`/`nssm` 且 CI 无法验证，超出本里程碑可测范围。
- 不引入新的配置文件格式（如 TOML）：配置仍走 env 变量 + 命令行覆盖参数，保持单用户自托管的最小心智负担。
- 不触碰前端、不改产品语义、不引入多用户/RBAC 概念。

## 子任务清单

| # | 内容 | 状态 |
|---|------|------|
| 1 | 共享 CLI 框架（`rex-common::cli`）：`Cli`/`Commands`/`ServiceCmd` clap derive 定义 + `dispatch()` 入口 | ⬜ |
| 2 | 服务安装抽象（`rex-common::service`）：平台检测 + systemd 单元生成 + launchd plist 生成 + install/uninstall/start/stop/restart/status（生成逻辑可单测） | ⬜ |
| 3 | Hub CLI 接入：重构 `crates/rex-hub/src/rex-hub.rs` 的 `main()`，先 clap 解析再分发 `run`(保留 supervisor/worker 分支)/`version`/`service` | ⬜ |
| 4 | Agent CLI 接入：重构 `crates/rex-agent/src/rex-agent.rs` 的 `main()`，复用同一套 CLI 与服务逻辑 | ⬜ |
| 5 | 配置注入与文档：`service install` 写入当前相关 env；更新部署文档（`docs/architecture` + 产品 §9 部署）说明开机自启 | ⬜ |
| 6 | 测试与收尾：单测（单元内容 / 平台检测 / plist XML 合法 / CLI 解析）+ fmt/clippy/test/build 全绿 | ⬜ |

## 子任务详细设计

### 1 共享 CLI 框架（`rex-common::cli`）

- **功能目标**：定义 Hub / Agent 共用的命令行结构，提供 `dispatch()` 入口，使两个二进制只需传入自己的 `run` 闭包（启动逻辑）即可复用全部子命令与 `service` 处理。
- **文件结构**（创建 / 修改）：
  - 新建 `crates/rex-common/src/cli.rs`
  - 在 `crates/rex-common/src/lib.rs` 导出 `pub mod cli;`
- **接口设计**（clap derive 数据模型）：
  ```rust
  #[derive(Parser)]
  #[command(name = "", about = "REX Hub/Agent", version = env!("CARGO_PKG_VERSION"))]
  pub struct Cli {
      #[command(subcommand)]
      pub command: Option<Commands>, // None = 默认 run
  }

  #[derive(Subcommand)]
  pub enum Commands {
      /// 启动服务（默认；等价于直接运行可执行文件）
      Run(RunOpts),
      /// 打印版本号
      Version,
      /// 注册/管理操作系统服务（开机自启）
      Service(ServiceCmd),
  }

  #[derive(clap::Args)]
  pub struct RunOpts {
      #[arg(long)] pub port: Option<u16>,
      #[arg(long)] pub data_dir: Option<PathBuf>,
      #[arg(long)] pub hub_url: Option<String>,
      #[arg(long)] pub token: Option<String>,
  }

  #[derive(Subcommand)]
  pub enum ServiceCmd {
      /// 安装为系统服务并启用开机自启
      Install(InstallOpts),
      /// 卸载系统服务
      Uninstall,
      /// 启动服务
      Start,
      /// 停止服务
      Stop,
      /// 重启服务
      Restart,
      /// 查看服务状态
      Status,
  }

  #[derive(clap::Args)]
  pub struct InstallOpts {
      /// 安装到用户级 scope（默认）；否则系统级（需 root）
      #[arg(long)] pub system: bool,
      /// 服务名（默认 rex-hub / rex-agent）
      #[arg(long)] pub name: Option<String>,
  }
  ```
  `dispatch` 签名：
  ```rust
  pub fn dispatch(
      cli: Cli,
      kind: ServiceKind,           // Hub | Agent
      run: impl FnOnce(RunOpts),   // 各二进制自己的 supervisor+worker 启动逻辑
  ) -> anyhow::Result<()>
  ```
  - `None` 或 `Some(Run)` → 调用 `run(opts)`（注：传入的 `port`/`data_dir`/`hub_url`/`token` 仅作记录/校验，真正生效仍通过 env；为不破坏 supervisor→worker 的 `REX_WORKER` 传递，本里程碑 `RunOpts` 只校验参数不强制注入，避免改动进程模型。实际 env 由运行环境（shell 或服务单元）提供）。
  - `Some(Version)` → `println!("rex-{} {}", kind.as_str(), env!("CARGO_PKG_VERSION"))` 后退出。
  - `Some(Service(cmd))` → 调用 `rex_common::service::handle(kind, cmd)`。
- **交互设计**：无前端，纯命令行。
- **后端流程**：`main()` 第一步 `Cli::parse()`（clap 自动处理 `--help`/`--version`），再 `dispatch`。
- **测试标准**：clap 解析单测（无参→默认 Run；`version`/`service install --system`/`service status` 解析正确）。
- **提交信息**：`feat(cli): add shared clap CLI framework for hub/agent`

### 2 服务安装抽象（`rex-common::service`）

- **功能目标**：跨平台生成操作系统服务单元并注册/启停，使「开机自启」成为一条命令。生成逻辑与「实际执行 systemctl/launchctl」解耦，便于单测。
- **文件结构**（创建）：
  - 新建 `crates/rex-common/src/service.rs`
  - 在 `lib.rs` 导出 `pub mod service;`
- **接口设计**：
  ```rust
  pub enum ServiceKind { Hub, Agent }
  impl ServiceKind { pub fn as_str(&self) -> &'static str; pub fn default_name(&self) -> &'static str; }

  pub enum ServicePlatform { Linux, Macos, Other }

  pub struct InstallConfig<'a> {
      pub kind: ServiceKind,
      pub name: String,            // 来自 InstallOpts.name 或默认
      pub user: bool,              // InstallOpts.system 取反
      pub exe: PathBuf,            // std::env::current_exe() 解析
  }

  pub fn detect_platform() -> ServicePlatform
  pub fn unit_path(cfg: &InstallConfig) -> PathBuf          // systemd 或 launchd 路径
  pub fn render_systemd_unit(cfg: &InstallConfig) -> String  // 生成 .service 文本
  pub fn render_launchd_plist(cfg: &InstallConfig) -> String // 生成 .plist 文本
  pub fn install(cfg: &InstallConfig) -> Result<String, ServiceError>
  pub fn uninstall(kind: ServiceKind, name: &str, user: bool) -> Result<String, ServiceError>
  pub fn start(kind, name, user) -> Result<String, ServiceError>
  pub fn stop(kind, name, user) -> Result<String, ServiceError>
  pub fn restart(kind, name, user) -> Result<String, ServiceError>
  pub fn status(kind, name, user) -> Result<String, ServiceError>
  pub fn handle(kind: ServiceKind, cmd: ServiceCmd) -> anyhow::Result<()> // dispatch 调用
  ```
- **systemd 单元生成**：
  ```ini
  [Unit]
  Description=REX Hub
  After=network.target

  [Service]
  Type=simple
  ExecStart=<exe>
  Environment=REX_PORT=3000
  Environment=REX_DATA_DIR=/home/<user>/.rex
  Restart=on-failure
  RestartSec=2

  [Install]
  WantedBy=multi-user.target      # system
  # WantedBy=default.target       # user
  ```
  - env 行来自当前进程 env（`REX_PORT`/`REX_DATA_DIR`/`REX_STATIC_DIR` for Hub；`REX_HUB_URL`/`REX_AGENT_TOKEN` for Agent），仅写入非空的变量，保持与手工启动一致。
  - user scope 路径：`$XDG_CONFIG_HOME/systemd/user/rex-<kind>.service`（默认 `~/.config/systemd/user/`）；system scope 路径：`/etc/systemd/system/rex-<kind>.service`。
  - `install()` 流程：`daemon-reload` → `enable` → `start`（user 模式加 `--user`）。
- **launchd plist 生成**：
  ```xml
  <?xml version="1.0" encoding="UTF-8"?>
  <!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
  <plist version="1.0">
  <dict>
    <key>Label</key><string>com.rex.<kind></string>
    <key>ProgramArguments</key><array><string><exe></string></array>
    <key>EnvironmentVariables</key><dict>
      <key>REX_PORT</key><string>3000</string>...
    </dict>
    <key>RunAtLoad</key><true/>
    <key>KeepAlive</key><true/>
    <key>StandardErrorPath</key><string>~/Library/Logs/rex-<kind>.log</string>
    <key>StandardOutPath</key><string>~/Library/Logs/rex-<kind>.log</string>
  </dict>
  </plist>
  ```
  - user scope：`~/Library/LaunchAgents/com.rex.<kind>.plist`；system scope：`/Library/LaunchDaemons/com.rex.<kind>.plist`（需 root）。
  - `install()`：`launchctl load <path>`（user）/ `launchctl load -w`（system，谨慎）+ `RunAtLoad` 已保证开机自启。
  - `start/stop`：`launchctl start/stop <label>`；`status`：`launchctl list | grep <label>` 解析。
- **Other 平台（如 Windows）**：`install`/`uninstall`/`start`/`stop`/`restart`/`status` 全部返回 `ServiceError::Unsupported`，消息含指引（用 nssm 或任务计划程序）。
- **错误处理**：用 `anyhow::Result`；执行外部命令失败返回带命令与 stderr 的 `ServiceError`。`ServiceError` 在 `rex-common` 内定义为 `anyhow` 上下文友好类型（或仅用 `anyhow::anyhow!` + 上下文）。
- **测试标准**：
  - `detect_platform()` 在 CI（Linux）返回 `Linux`（用 cfg 也可）。
  - `render_systemd_unit` 输出含 `[Unit]`/`ExecStart=<exe>`/`Restart=on-failure`/`WantedBy=` 且 user 与 system 的 `WantedBy` 不同。
  - `render_launchd_plist` 输出是合法 XML（用 `std::str` 校验含 `<plist` 与 `RunAtLoad`，并能被 `plist` 解析库或简单结构校验；若无 plist 库则用 XML 片段断言 + 标签配对检查）。
  - env 注入：传入 `REX_PORT=3000` 时单元/plist 含对应行。
- **提交信息**：`feat(service): add cross-platform service install (systemd + launchd)`
- **设计原则**：生成逻辑（render_*）与执行（install/start/...）分离，render 纯函数易测；执行部分在测试中以 `cfg(unix)` 跳过真实 `systemctl`，仅验证路径解析与命令构造。

### 3 Hub CLI 接入

- **功能目标**：`rex-hub` 入口先用 clap 解析，再分发到 run/version/service。
- **文件结构**（修改）：`crates/rex-hub/src/rex-hub.rs`
- **接口设计**：重构 `main()`：
  ```rust
  fn main() {
      let cli = rex_common::cli::Cli::parse();
      let kind = rex_common::service::ServiceKind::Hub;
      let result = rex_common::cli::dispatch(cli, kind, |_opts| {
          // 保留原有 supervisor/worker 分支
          if std::env::var("REX_WORKER").is_err() {
              supervisor_main();
          } else {
              worker_main();
          }
      });
      if let Err(e) = result {
          eprintln!("Error: {e:#}");
          std::process::exit(1);
      }
  }
  ```
  - `supervisor_main()` / `worker_main()` 保持原样（含 tracing 初始化、data_dir、端口解析）。
  - `version` / `service` 由 `dispatch` 处理，不再需要 `main` 自己判断 env（但 `REX_WORKER` 分支仍在 `run` 闭包内，保证 worker 子进程由 supervisor 以 `REX_WORKER=1` 拉起时走 worker 路径）。
- **交互设计**：无前端。
- **后端流程**：见上。
- **测试标准**：复用 `rex-common::cli` 解析测试；Hub 集成主要靠 `cargo build` 跑通 + `rex-hub --help` / `rex-hub version` 手动验证（CI 可加一个 `cargo run --bin rex-hub -- --version` 烟雾测试，可选）。
- **提交信息**：`feat(hub): wire clap CLI into hub entrypoint`

### 4 Agent CLI 接入

- **功能目标**：`rex-agent` 入口接入同一套 CLI；`run` 走 Agent 自身 supervisor/worker（`rex-agent::supervisor::run_supervisor` + `worker_main`）。
- **文件结构**（修改）：`crates/rex-agent/src/rex-agent.rs`
- **接口设计**：
  ```rust
  fn main() {
      let cli = rex_common::cli::Cli::parse();
      let kind = rex_common::service::ServiceKind::Agent;
      let result = rex_common::cli::dispatch(cli, kind, |_opts| {
          if std::env::var("REX_WORKER").is_ok() {
              worker_main();
          } else {
              crate::supervisor::run_supervisor();
          }
      });
      if let Err(e) = result { eprintln!("Error: {e:#}"); std::process::exit(1); }
  }
  ```
  - `worker_main()` 保持原样；`service install` 对 Agent 注入 `REX_HUB_URL`/`REX_AGENT_TOKEN`（来自当前 env）。
- **交互设计**：无前端。
- **后端流程**：见上。
- **测试标准**：同子任务 3。
- **提交信息**：`feat(agent): wire clap CLI into agent entrypoint`

### 5 配置注入与文档

- **功能目标**：`service install` 把当前相关 env 写进单元；更新部署文档。
- **文件结构**（修改）：
  - `crates/rex-common/src/service.rs`（`render_systemd_unit`/`render_launchd_plist` 读取 env）
  - `docs/architecture/process-model.md`（新增「开机自启」小节，引用 `service install`）
  - `docs/PRODUCT.md` §9 部署（补充二进制部署下的 `service install` 开机自启说明；不改产品语义）
  - 必要时 `docs/agent-readme.md`（若存在，补充 agent 注册为服务）
- **接口设计**：env 采集集中在 `InstallConfig` 构造处：
  ```rust
  fn current_env_for(kind: ServiceKind) -> Vec<(String, String)> {
      match kind {
          Hub => pick(["REX_PORT","REX_DATA_DIR","REX_STATIC_DIR"]),
          Agent => pick(["REX_HUB_URL","REX_AGENT_TOKEN"]),
      }
  }
  ```
  （`pick` 只保留非空的 env）。
- **文档要点**：Linux `rex-hub service install`（用户级）即开机自启；系统级加 `--system`（root）；macOS 同构；Windows 给 nssm 指引。强调「不影响现有 env 配置方式」。
- **测试标准**：文档不写测试；代码侧 env 注入已在子任务 2 单测覆盖。
- **提交信息**：`docs: document service install / auto-start for hub and agent`

### 6 测试与收尾

- **功能目标**：质量门禁全绿。
- **文件结构**：`crates/rex-common/src/service.rs`（追加 `#[cfg(test)]`）、`cli.rs`（追加测试）
- **接口设计**：见各子任务测试标准。
- **后端流程**：`cargo fmt --check` / `cargo clippy --workspace --all-targets` / `cargo test --workspace`。
- **测试标准**：新增单测覆盖 `detect_platform`、`render_systemd_unit`（user/system、env 注入）、`render_launchd_plist`（XML 结构、`RunAtLoad`/`KeepAlive`）、`Cli` 解析（`None`→Run、`version`、`service install --system`、`service status`）。
- **提交信息**：`test: add unit tests for cli and service module`

## 设计核对点

- 是否仍遵守「单用户、自托管、无 RBAC」：是，CLI/服务仅运维本机进程。
- 是否改动进程模型 / 退出码 / 更新机制：否，supervisor+worker 与 `REX_WORKER` 分支完全保留。
- 文件传输是否不经浏览器：不涉及。
- 是否引入不该有的概念：否。
- 是否跳阶段：否，本功能为已稳定产品的运维增强，属 minor。
- 版本一致：Hub/Agent 共用同一 `CARGO_PKG_VERSION`，CLI 与 service 模块在 `rex-common`，天然一致。
- 平台边界：Linux systemd + macOS launchd 实现并测试生成逻辑；Windows 仅返回 Unsupported 错误，不崩溃。

## Flow Status

- [x] 步骤1：编写里程碑文档
- [x] 步骤2：设计核对
- [ ] 步骤3：开发
- [ ] 步骤4：代码精简
- [ ] 步骤5：代码审查
- [ ] 步骤6：测试验证
- [ ] 步骤7：设计再确认
- [ ] 步骤8：提交

## 打回记录

| 时间 | 步骤 | 原因 |
|------|------|------|
| | | |

## Bugs

| 状态 | 优先级 | 标题 | 来源 | 描述 |
|------|--------|------|------|------|
