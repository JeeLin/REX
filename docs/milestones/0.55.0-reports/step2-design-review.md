# 步骤2：设计核对报告

## 里程碑：0.55.0 前端 i18n 与主题系统优化

## 审查维度

### 产品定位检查

| 维度 | 检查项 | 结论 |
|------|--------|------|
| 单用户 | 不引入多用户/RBAC | ✅ 纯前端重构，无用户系统变更 |
| 自托管 | 不依赖外部服务 | ✅ 所有改动本地运行 |
| 架构一致 | 不改变进程模型 | ✅ 不涉及后端 |

### 子任务合理性检查

| 子任务 | 设计合理性 | 风险评估 | 结论 |
|--------|-----------|----------|------|
| 1. i18n 重复 key 清理 | 消除维护负担，统一命名空间 | 低风险：纯 key 重命名 | ✅ |
| 2. 系统主题自动切换 | 补 PRODUCT.md 3.12 描述的功能 | 低风险：添加 matchMedia 监听 | ✅ |
| 3. Dashboard bug 修复 | 修复功能不符合设计的行为 | 低风险：修改一处函数调用 | ✅ |
| 4. settings.ts 重构 | 架构一致性改进 | 中风险：涉及多组件引用更新 | ⚠️ 需注意 |

### 产品语义检查

| 检查项 | 结论 |
|--------|------|
| 是否改变用户可见行为 | 仅子任务3修复了一个 bug（"在新标签中打开"现在真正打开新标签） |
| 是否引入新概念 | 否 |
| 是否跳阶段 | 否 |

### 设计细节检查

| 检查项 | 结论 |
|--------|------|
| 子任务1：保留哪个命名空间 | 设计明确保留 `ws.*`，删除 `ws.workspace.*`，方向正确 |
| 子任务2：matchMedia 生命周期 | 提到组件卸载时移除监听器，考虑了内存泄漏 |
| 子任务3：实现方案 | 提供了两种方案（addTab vs 跳过去重），需要开发时确认 |
| 子任务4：重构范围 | 提到保持现有函数签名，避免大规模改动，风险可控 |

## 小问题

1. **子任务2 commit message 标记为 `feat:`** — 但里程碑版本类型是 patch，且描述中说"不添加新功能"。建议改为 `fix:` 或 `refactor:`。这是措辞问题，Claude 直接修正。

2. **子任务3 缺少 `connectToResource` 的具体分析** — 设计提到"参考 useProtocol.ts"但未给出具体实现差异。这是细节缺失，开发时会分析。

## 结论

✅ **通过**。所有子任务设计合理，风险可控，产品语义未改变。2 个小问题（措辞、细节）在开发阶段修正。
