# M26 步骤2：设计核对报告

## 审查维度

### 1. 产品文档一致性（PRODUCT.md §3.7）

| 检查项 | M26 设计 | PRODUCT.md §3.7 | 结果 |
|--------|----------|-----------------|------|
| SQL 历史记录 | 右键菜单"历史记录"→ 底部面板，最近 100 条 | §3.7 右键菜单：「历史记录 | 打开最近执行的 SQL 历史列表」 | ✅ |
| Ctrl+S 保存 | 编辑器快捷键 Ctrl+S → 触发保存事件 | §3.7 工具栏：「Ctrl+S 保存」；快捷键表：「Ctrl+S 保存查询文件」 | ✅ |
| 结果复制按钮 | footer 添加📋复制按钮（TSV 格式） | §3.7 结果区：「📋 复制、⬇ 导出、JSON · CSV 格式切换」 | ✅ |

### 2. 架构一致性

| 检查项 | M26 设计 | 架构约束 | 结果 |
|--------|----------|----------|------|
| API 模式 | `/api/resources/:resource_id/sql/history` | 与 M25 查询文件 API 模式一致 | ✅ |
| 文件存储 | `{data-dir}/sql-history/{resource_id}.json` | 文件数据不经过浏览器 | ✅ |
| 路径安全 | `sql_history_dir()` 校验 resource_id | 与 M25 `queries_dir()` 同模式 | ✅ |

### 3. 产品边界

| 检查项 | 结果 |
|--------|------|
| 不引入执行计划（需要数据库引擎支持） | ✅ 未引入 |
| 不引入 AI 助手 | ✅ 未引入 |
| 不引入全局查询（跨数据库） | ✅ 未引入 |
| 不引入多用户概念 | ✅ 未引入 |
| 单用户、自托管 | ✅ 保持 |

### 4. 子任务拆分粒度

| 子任务 | 粒度评估 | 结果 |
|--------|----------|------|
| 26.1 后端历史 API | 单一功能点（存储 + CRUD） | ✅ |
| 26.2 前端历史面板 | 单一功能点（组件 + 集成） | ✅ |
| 26.3 快捷键 + 复制按钮 | 两个小改动，合并为一个 commit | ✅ |

### 5. CLAUDE.md 审查维度

| 维度 | 检查结果 |
|------|----------|
| 正确性 | 设计逻辑清晰，数据模型合理 |
| 安全性 | 路径安全校验复用已有模式，无路径逃逸风险 |
| 架构一致性 | API 模式、存储模式与 M25 一致 |
| 测试覆盖 | 每个子任务有明确测试标准 |
| 错误处理 | 文件 I/O 使用 anyhow，HTTP 错误使用 axum 标准 |
| 配置和密钥处理 | 不涉及 |
| 审计日志 | 历史记录本身是审计数据，不需额外审计 |
| 里程碑文档一致性 | 设计与文档完全一致 |

## 结论

✅ **通过**。M26 设计与 PRODUCT.md §3.7 一致，架构模式与现有代码一致，产品边界未被污染，子任务拆分粒度合理。

**无打回项**。
