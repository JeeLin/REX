# M6 设计核对报告

## 审查范围

里程碑文档 `docs/milestones/M6-update-detection.md` vs 产品文档 `docs/PRODUCT.md` + 开发文档 `docs/DEVELOPMENT.md`

## 核对维度

### 1. 是否符合产品定位（单用户、自托管）

✅ 通过。更新检查仅限 Hub 自身和 Agent 版本，不涉及多用户或多租户概念。

### 2. 架构是否一致（单二进制 + supervisor + worker）

✅ 通过。M6 只实现检测和展示，不改变进程模型。更新检测在 worker 中执行，符合 PRODUCT.md 阶段 1 设计。

### 3. 文件传输是否不经过浏览器

✅ 通过。M6 不涉及文件传输。

### 4. 是否引入了不该有的概念

✅ 通过。不引入自动下载/替换（M7 才做）、不引入 RBAC、不引入多用户。

### 5. 是否跳阶段实现

✅ 通过。严格限制在"检测 + 提示"，不实现自动更新/替换/回滚。

### 6. 是否把实现细节写进了产品文档

✅ 通过。里程碑文档包含接口设计等实现细节，产品文档未被修改。

### 7. 与 DEVELOPMENT.md 里程碑 7 的一致性

| DEVELOPMENT.md 任务 | M6 子任务 | 一致性 |
|---------------------|-----------|--------|
| Hub worker 定期查询 GitHub Releases | 6.2 GitHub Releases 更新检查器 | ✅ |
| 解析当前平台对应 release asset | 6.2 版本比较 | ✅ |
| 比较当前版本和最新版本 | 6.2 版本比较 | ✅ |
| 设置页显示"有新版本" | 6.5 设置页更新区块 | ✅ |
| Agent 页面显示 Hub/Agent 版本总览 | 6.6 Agent 页面版本总览 | ✅ |
| Agent 心跳上报版本和 SHA256 | 6.3 Agent 心跳上报版本 | ✅ |
| Hub 判断 Agent 是否需要更新 | 6.3 Hub 版本对比 | ✅ |

### 8. 完成标准一致性

| DEVELOPMENT.md 完成标准 | M6 设计覆盖 |
|------------------------|-------------|
| Hub 能检测新版本 | ✅ 6.1 + 6.2 |
| 设置页展示更新提示 | ✅ 6.5 |
| Agent 页面展示版本总览 | ✅ 6.6 |
| 不自动下载或替换二进制 | ✅ 明确标注"不做什么" |

## 结论

✅ **通过** — 里程碑文档与产品文档、开发文档一致，设计合理，无超前实现。
