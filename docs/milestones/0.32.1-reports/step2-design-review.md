# 0.32.1 步骤2：设计核对报告

## 审查范围

里程碑文档 vs 产品文档，验证 bug 修复设计是否合理。

## 审查维度

### ✅ 产品对齐

- 所有 bug 修复都在产品文档定义的功能范围内
- 不涉及多用户/RBAC
- 不引入新协议或新功能
- 保持单用户自托管定位

### ✅ 架构一致性

- 分屏拖拽：纯前端修复，不涉及后端 API
- SSH 复制粘贴：前端 xterm.js 配置修复
- SQLite 连接：前端错误处理 + 可能需要后端状态检查
- 仪表盘/API：前端 API 路径修复 + i18n
- Agent 页面：后端状态判断修复 + 前端 UI 修复
- 快捷键展示：前端 CSS/模板修复

### ✅ 子任务粒度

6 个子任务，每个聚焦一个独立问题，粒度合理。

### ✅ 关键发现

**Agent 状态 bug 根因**：`agent.rs:172` 新建 agent 记录时直接设 `status = 'online'`，但新 agent 还未连接 Hub，应设为 `offline`。

## 结论

设计合理，关键 bug 根因已识别。✅
