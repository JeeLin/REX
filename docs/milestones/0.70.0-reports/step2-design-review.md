# 步骤2 设计核对：0.70.0 (SIP 电话资源基础)

## 审查维度

| # | 维度 | 结论 | 说明 |
|---|------|------|------|
| 1 | 是否符合产品定位（单用户、自托管） | ✅ | `sip` 资源归单一用户本地管理；文档显式声明不引入多用户/RBAC/企业协作，与 PRODUCT.md §1 定位一致。 |
| 2 | 架构是否一致（单二进制 + supervisor + worker） | ✅ | baresip 以进程内 FFI 运行于 Hub worker 与 Agent worker（非子进程 CLI）；`rex-sip` 两二进制共用；Agent 侧复用 `agent_ws.rs` 的 channel_id 多路复用 + binary 帧，未新造隧道。 |
| 3 | 文件传输是否不经过浏览器 | ✅ | M82a 仅信令层，流量路径 Hub↔Agent↔SIP server，不经浏览器；浏览器实时音频（产品原则「媒体不经过浏览器」的显式例外）已正确划到 0.70.1，不在本里程碑范围。 |
| 4 | 是否引入了不该有的概念（RBAC、多用户） | ✅ | 文档在产品边界与「不做什么」中显式排除 RBAC/多用户/企业协作。 |
| 5 | 是否跳阶段实现 | ✅ | M82a 做信令地基；音频(0.70.1)、通话记录/录音/抓包/展示图(0.70.2) 依次递延。`SipUa::on_rtp` 仅预留空桩与接口，属接口预留非提前实现，未越界。 |
| 6 | 是否把实现细节写进了产品文档 | ✅ | baresip FFI、channel_id 多路复用、build.rs+CMake、连接模型等实现细节均收敛于里程碑文档；PRODUCT.md 未被本次改动触及。 |

## 汇总

- **通过维度**：6/6
- **结论**：✅ 通过

## 发现的问题

无

## 备注（非阻塞观察）

- PRODUCT.md §2.3（行 76-86）协议表当前仍为 7 种，待 M82a 完成（步骤8）时应将 SIP 补为第 8 种协议（属产品事实更新，非实现细节，不违反维度 6）。
- M82b 浏览器实时音频属于产品原则「媒体不经过浏览器」的显式例外，届时需在 PRODUCT.md 相应位置记录该例外，属后续里程碑工作。
