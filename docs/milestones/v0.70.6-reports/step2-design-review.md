# 步骤2 设计核对：v0.70.6 统一 Hub↔Agent 隧道架构

## 审查维度

> 维度来源：CLAUDE.md「和设计核对」核对项 + PRODUCT.md §1 产品定位 / §8 技术架构 / §10 设计核对基线（AGENTS.md 未定义独立 `## 审查维度`，依 devflow-review 回退到上述项目约定维度）。

| # | 维度 | 结论 | 说明 |
|---|------|------|------|
| 1 | 产品定位（单用户/自托管/深色优先） | ✅ | v0.70.6 未引入多用户、RBAC、企业协作概念；属后端架构下沉，与「个人自托管」定位一致 |
| 2 | 架构一致（单二进制 + supervisor + worker） | ✅ | 文档「产品边界 · 不做什么」显式声明不改动进程模型；Agent 内嵌协议客户端仍在 worker 内执行 |
| 3 | 文件传输不过浏览器（§8 硬性约束） | ✅ | 子任务6（Agent 终结 S3/SFTP）：Agent 直接对目标做分块读写，`FileChunk` 仅经隧道传元数据/内容分块，满足「数据在 Hub/Agent/远端之间传输，不经过浏览器」 |
| 4 | 不引入不该有的概念 | ✅ | 文档明确排除多用户/RBAC/企业协作；不新增资源类型（仍 8 种） |
| 5 | 不跳阶段 | ✅ | 线性推进 M82f(0.70.5) → v0.70.6；无跨阶段 |
| 6 | 不污染产品文档 | ✅ | 实现细节写入里程碑文档；PRODUCT.md 在本里程碑保持不变 |
| 7 | 协议 crate 下沉可行性（§8 共享 crate） | ✅ | §8 声明 rex-ssh/rex-mysql/rex-postgresql/rex-redis/rex-sqlite/rex-s3 为 Hub/Agent 共享 crate；Agent 复用这些 crate 跑协议，无架构冲突 |
| 8 | 版本一致（无跨版本兼容） | ✅ | v0.70.6 同步 bump Hub + Agent；沿用「Hub/Agent 版本须一致」约束 |
| 9 | 复用单 WS 隧道范式（§8 通信协议） | ✅ | Hub↔Agent 仍为单条 WebSocket(TLS) + `channel_id` 多路复用；未新建通道，仅把协议终结从 Hub 下沉到 Agent |
| 10 | 设计核对基线（§10 不可引入项） | ✅ | 本里程碑为后端协议下沉，前端连接树/工作空间交互无变化，未触碰 §10 对标交互模式清单 |

## 汇总

- **通过维度**：10/10
- **结论**：✅ 通过

## 发现的问题

无。

---

### 审查备注（非阻塞，供步骤3/7 关注）

- **凭据信任边界**：`connect` 消息中 `password`/`privateKey` 经加密 WS 明文下发 Agent 是既有实现（见 `terminal_ws.rs` 现有下发逻辑）。v0.70.6 子任务7 仅「明确信任边界」，不引入新攻击面，但步骤7 再确认时应核对 Agent 侧凭据不在日志中明文落盘。
- **向后兼容**：本次为 agent 模式协议层重构，直连模式行为须保持不变（子任务4/5/6 均标注「直连模式行为不变」），步骤6 测试需覆盖直连回归。
