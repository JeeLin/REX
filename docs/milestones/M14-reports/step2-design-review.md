# M14 步骤2：设计核对报告

## 审查维度

### 1. 产品一致性
- ✅ 单用户、自托管：无多用户/RBAC 概念引入
- ✅ 文件传输不经过浏览器：M14 只嵌入现有 UI 组件，不改变传输机制
- ✅ 深色优先：使用现有 CSS 变量，兼容深色/浅色主题
- ✅ 无新产品概念引入

### 2. 架构一致性
- ✅ 面板组件放在 `features/workspace/panels/`，符合功能域组织
- ✅ 复用现有组件（SqlEditor、FileList、FileBreadcrumb 等），不重复造轮子
- ✅ useTabs.ts 扩展方式与 M13 一致（模块级单例 ref）
- ✅ i18n 键在 zh.ts 和 en.ts 中均有定义

### 3. 原型一致性
- ✅ SSH 面板结构与 terminal.html 一致（工具栏 + 终端 + 状态栏）
- ✅ SQL 面板结构与 sql.html 一致（标签 + 编辑器 + 结果网格）
- ✅ Files 面板结构与 files.html 一致（面包屑 + 文件列表 + 工具栏）
- ✅ 面板渲染逻辑清晰（协议 → 组件映射）

### 4. 边界检查
- ✅ 不重构现有独立页面（Terminal.vue / SqlConsole.vue / Files.vue 保持不变）
- ✅ 不实现 WebSocket 终端连接（后端 API 层面）
- ✅ 不实现 SSH 内置 SFTP split view
- ✅ 不实现面板间拖拽标签交换
- ✅ 不跳阶段（不引入新协议 crate 或后端 API）

### 5. 子任务拆分
- ✅ 4 个子任务粒度合理，每个 1 个 commit
- ✅ 14.1-14.3 是独立的面板组件，互不依赖
- 14.4 依赖 14.1-14.3（渲染逻辑需要面板组件存在）

### 6. 潜在风险
- 🟡 面板组件从独立页面提取逻辑时，需确保不破坏独立页面功能
- 🟡 分屏模式下多个 xterm.js 实例同时运行可能有性能影响

## 结论

✅ 里程碑文档与产品文档一致，子任务拆分合理，边界清晰。潜在风险为已知工程问题，不影响设计方向。
