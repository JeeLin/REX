# 缺陷池

| 提出版本 | 优先级 | 标题 | 来源 | 描述 |
|----------|--------|------|------|------|
| v0.82.0 | 🟡 | Agent 健康状态展示直连环境 | 缺陷池 | Agent 健康状态页面展示了直连的环境，而非通过 Agent 隧道连接的环境；健康状态应仅展示 Agent 实际管理的资源，避免混淆 |
| v0.82.0 | 🟢 | 顶部导航栏 Workspace 按钮改为全局搜索 | 缺陷池 | 顶部导航栏的 Workspace 按钮功能单一（仅跳转 /workspace），建议改为全局搜索栏（Cmd+K），支持快速跳转资源、SSH 连接、数据库、Redis、设置项等，提升导航效率 |
| v0.82.0 | 🟢 | 拓扑连线布局优化建议 | 缺陷池 | 拓扑视图连线目前使用直线，多条连线会重叠交叉；建议改为曲线/弧线布局，节点围绕中心 Hub 散射分布，避免连线互相排斥重叠 |