# 缺陷池

| 提出版本 | 优先级 | 标题 | 来源 | 描述 |
|----------|--------|------|------|------|
| v0.73.0 | 🔴 | Agent SSH 连接后无法输入命令，后续连接全部失败 | 用户反馈 | 通过 Agent 连接 SSH 时，首次连接建立后无法输入命令，后续连接全部报 "SSH connection failed"。日志显示首条 `AGENT_SSH_CONNECTED` 成功，但之后连续 `AGENT_SSH_FAILED`。用户无法排查。 |
